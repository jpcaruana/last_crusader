language: elixir

elixir:
  - 1.10
otp_release:
  - 23.0

before_script:
  - mix deps.get
script:
  - mix test
script:
  - mix docs

#script:
#  - MIX_ENV=test mix do compile --warnings-as-errors, coveralls.json

#after_success:
#  - bash <(curl -s https://codecov.io/bash)
